project_name = "filebuddy"

[tasks.build]
command = "swift build"
description = "Build the Swift package"

[tasks.format]
command = "swift format --in-place --recursive Sources/"
description = "Format Swift source files"
